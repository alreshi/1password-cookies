## Summary

<!-- Why this change exists. Do not paste cookie values or tokens. -->

## Checklist

- [ ] `SKILL.md` stays under 500 lines; detail is in `references/`
- [ ] No secrets, cookie JSON, vault ids, or item ids from a real vault
- [ ] CHANGELOG updated if behavior changed
- [ ] Scripts still send secrets through `--template`, not argv
